package com.github.alonwang.linkedlist

import spock.lang.Specification

class LinkedListSpecification extends Specification {
    //TODO 解析Node
//    def "test reverselist"() {
//        given:
//        def head = new ListNode(1);
//        head.next = new ListNode(2);
//        head.next.next = new ListNode(3);
//        expect:
//        new ReverseList().reverseList(head);
//        def result = [3, 2, 1];
//        result.forEach({ it -> head.val == it; head = head.next })
//    }
}
